import { InputResetDirectiveDirective } from './input-reset-directive.directive';

describe('InputResetDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InputResetDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
